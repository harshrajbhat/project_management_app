<% content_for :title, "New Team" %>

<div class="container mt-3 mb-3 w-100" style="max-width: 800px;">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="text-primary">New Team</h1>
    <div>
      <%= link_to teams_path, class: "btn btn-secondary" do %>
        <i class="bi bi-arrow-left"></i> Back to Teams
      <% end %>
    </div>
  </div>

  <div class="card shadow rounded">
    <div class="card-body">
      <%= render "form", team: @team %>
    </div>
  </div>
</div>
